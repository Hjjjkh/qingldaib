# Cloudflare Pages 配置
# 注意：这是一个 Cloudflare Pages 项目，不是 Workers 项目
# 构建和部署由 Cloudflare Pages 自动处理
# 
# D1/KV/R2 绑定在 Cloudflare Dashboard 配置：
# Settings → Functions → Function bindings
#
# 环境变量在 Cloudflare Dashboard 配置：
# Settings → Environment variables
